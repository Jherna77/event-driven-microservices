# Application configuration for development environment
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sentinelDB
    username: sentinel
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
